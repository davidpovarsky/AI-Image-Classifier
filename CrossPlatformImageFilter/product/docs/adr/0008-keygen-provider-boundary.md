# ADR 0008: Keygen provider boundary

Status: accepted. Licensing uses a provider trait, device keypair, signed offline claims, and mock CI implementation. Provider administrator credentials never enter clients.
