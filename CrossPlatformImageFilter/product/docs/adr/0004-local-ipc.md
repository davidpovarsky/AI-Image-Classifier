# ADR 0004: Local IPC instead of HTTP

Status: accepted. Use named pipes/local sockets with peer identity, bounded typed messages, authorization, and replay resistance. A localhost administration server would enlarge the browser/network attack surface and is not used.
