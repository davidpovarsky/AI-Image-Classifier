{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "The main window may invoke only application-owned typed commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
